# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local probe output. These contain account identifiers -- serial number,
# owner email, dealer details, regKey, IP -- and must never be committed.
# Root-anchored so the integration's own JSON files are unaffected.
/spa.json
/spa*.json
/*dump*.json
/*.har

# macOS
.DS_Store
controlmyspa-*.zip

# Local session notes and reference, not part of the project
HANDOFF.md
API.md
